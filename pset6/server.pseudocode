lookup function
find the last "." in the path with strrchar() that returns a pointer to that char.
compare extensions found in path with knowed extensions and return ex. "text/css"

parse function
get method-request
search for get method
search for http/1.1

